async function e(){try{let e=`koalastuff_github_repos`,t=null,n=null;try{n=localStorage.getItem(e)}catch{}if(n){let e=JSON.parse(n);Date.now()-e.timestamp<360*60*1e3&&(t=e.data)}if(!t){let n=await fetch(`https://api.github.com/users/Shik3i/repos?sort=updated&per_page=100`);if(!n.ok)return;t=await n.json();try{localStorage.setItem(e,JSON.stringify({timestamp:Date.now(),data:t}))}catch{}}let r=new Map;t.forEach(e=>{r.set(e.full_name.toLowerCase(),new Date(e.pushed_at||e.updated_at))}),document.querySelectorAll(`.github-last-updated`).forEach(e=>{let t=e.getAttribute(`data-github-repo`);if(!t)return;let n=r.get(t.toLowerCase());if(n){let t=Math.floor((Date.now()-n.getTime())/(1e3*60*60*24)),r=new Intl.RelativeTimeFormat(document.documentElement.lang||`en`,{numeric:`auto`}),i=r.format(-t,`day`);t>=30&&t<365?i=r.format(-Math.floor(t/30),`month`):t>=365&&(i=r.format(-Math.floor(t/365),`year`)),e.innerHTML=`<span class="update-dot"></span>${i}`,e.classList.add(`has-fresh-data`)}})}catch(e){console.error(`Failed to fetch github data`,e)}}document.addEventListener(`astro:page-load`,e);